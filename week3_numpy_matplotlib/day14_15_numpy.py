# Problem 6:
# You have 5 pipe segments with lengths and diameters
# lengths = [1, 2, 3, 4, 5] m
# diameters = [0.05, 0.04, 0.03, 0.02, 0.01] m
# velocity = 2.0, friction = 0.02, density = 1000
# Calculate pressure drop for each segment
# dP = friction * (length/diameter) * (density * velocity^2 / 2)
# Find total pressure drop across all segments