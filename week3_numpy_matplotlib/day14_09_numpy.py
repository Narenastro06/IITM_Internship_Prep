import numpy as np
values = np.array([1, 4, 9, 16, 25])
print(np.sqrt(values)) # square root of each element
print(np.log(values)) # natural log of each element
print(np.exp(values)) # e^x of each element